# Generated migration for chatbot app.
# The chatbot app has no models — this empty initial migration satisfies
# Django's requirement that every INSTALLED_APPS entry with a migrations/
# package has at least one migration, so `manage.py migrate --check` does
# not raise warnings about the app being unmigrated.
from django.db import migrations


class Migration(migrations.Migration):

    initial = True

    dependencies = []

    operations = []
