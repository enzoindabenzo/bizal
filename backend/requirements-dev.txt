# Dev/test dependencies. Referenced by Dockerfile.dev only — NOT installed in production.
# M-3 FIX: Extracted from requirements.txt so these packages are absent from the
# production image. flower exposes a Celery monitoring UI on :5555 with no auth by
# default; coverage/pytest add ~50MB and unnecessary attack surface.
-r requirements.txt
# pytest is pinned to 8.4.2, not 8.2.2, specifically because locust==2.46.2
# requires pytest>=8.3.3 — 8.2.2 (the original pin) makes this file
# uninstallable as soon as locust is added. Verified this combination
# resolves and installs cleanly; re-check with
# `pip install --dry-run -r requirements-dev.txt` if bumping any of these.
coverage==7.6.1
flower==2.0.1
pytest==8.4.2
pytest-django==4.8.0
pytest-cov==5.0.0
locust==2.46.2
